import 'package:flutter/material.dart';

class AppColors {
  // Primary (Medical Blue/Teal)
  static const Color primary = Color(0xFF0056D2); // Deep professional blue
  static const Color primaryDark = Color(0xFF003DA5);
  static const Color primaryLight = Color(0xFFE6F0FF);
  static const Color primaryBg = Color(0xFFF0F5FF);

  // Secondary (Teal/Green - Health & Calm)
  static const Color secondary = Color(0xFF00BFA5);
  static const Color secondaryDark = Color(0xFF008E76);
  static const Color secondaryLight = Color(0xFFE0F7FA);

  // Status Colors (Medical Grade)
  static const Color success = Color(0xFF27AE60);
  static const Color successBg = Color(0xFFEAFBF1);

  static const Color warning = Color(0xFFF39C12);
  static const Color warningBg = Color(0xFFFEF5E7);

  static const Color error = Color(0xFFE74C3C);
  static const Color errorBg = Color(0xFFFDEDEC);

  static const Color info = Color(0xFF3498DB);
  static const Color infoBg = Color(0xFFEBF5FB);

  // Neutral / Background (Light)
  static const Color background = Color(0xFFF7F9FC); // Cool gray tint
  static const Color surface = Colors.white;
  static const Color scaffoldBackground = Color(0xFFF7F9FC);

  // Neutral / Background (Dark)
  static const Color backgroundDark = Color(0xFF1A1C1E);
  static const Color surfaceDark = Color(0xFF2B3035);
  static const Color scaffoldBackgroundDark = Color(0xFF121212);

  // Text (Light Theme)
  static const Color textPrimary = Color(0xFF1E293B); // Slate 800
  static const Color textSecondary = Color(0xFF64748B); // Slate 500
  static const Color textLight = Color(0xFF94A3B8); // Slate 400
  static const Color textWhite = Colors.white;
  static const Color textHint = Color(0xFF94A3B8);

  // Text (Dark Theme)
  static const Color textPrimaryDark = Color(0xFFF1F5F9);
  static const Color textSecondaryDark = Color(0xFF94A3B8);

  // Borders & Dividers
  static const Color border = Color(0xFFE2E8F0); // Slate 200
  static const Color divider = Color(0xFFF1F5F9);

  static const Color borderDark = Color(0xFF444444);
  static const Color dividerDark = Color(0xFF333333);

  // Shadows
  static const Color shadow = Color(0x14000000); // Soft shadow

  // Specific UI Elements
  static const Color iconDefault = Color(0xFF64748B);
  static const Color inputFill = Color(0xFFFFFFFF);
  static const Color inputBorder = Color(0xFFE2E8F0);
}
